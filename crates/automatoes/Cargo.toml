[package]
name = "automatoes"
version = "0.0.1"
edition = "2021"
rust-version = "1.85"

[lib]
name = "automatoes"
path = "src/lib.rs"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = [
    "blocking",
    "rustls-tls",
] }
serde_json = "1"
thiserror = "2"
