[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "rust-backend"
version = "0.1.0"
description = "Minimal Python extension backed by Rust"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
